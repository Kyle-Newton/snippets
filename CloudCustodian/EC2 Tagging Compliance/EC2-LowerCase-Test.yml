policies:
  - name: ec2-service-transform-lower
    resource: ec2
    actions:
      - type: normalize-tag
        key: Application
        action: lower
      - type: normalize-tag
        key: Owner
        action: lower
